const express = require("express"); const c = require("../../controllers/safePlace/access.controller"); const authenticate = require("../../middlewares/authenticate.middleware"); const validateBody = require("../../middlewares/validate.middleware"); const { emptySchema } = require("../../validations/safePlace.validation"); const router = express.Router(); router.get("/charter", c.charter); router.get("/access", authenticate, c.access); router.post("/charter/accept", authenticate, validateBody(emptySchema), c.accept); router.post("/charter/withdraw", authenticate, validateBody(emptySchema), c.withdraw); module.exports = router;
