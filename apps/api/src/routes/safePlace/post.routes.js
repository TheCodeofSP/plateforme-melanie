const express = require("express"); const p = require("../../controllers/safePlace/post.controller"); const c = require("../../controllers/safePlace/comment.controller"); const r = require("../../controllers/safePlace/reaction.controller"); const authenticate = require("../../middlewares/authenticate.middleware"); const access = require("../../middlewares/safePlaceAccess.middleware"); const validateBody = require("../../middlewares/validate.middleware"); const validateParams = require("../../middlewares/validateParams.middleware"); const validateQuery = require("../../middlewares/validateQuery.middleware"); const v = require("../../validations/safePlace.validation"); const createRateLimit = require("../../middlewares/rateLimit.middleware"); const router = express.Router(); router.use(authenticate, access); router.get("/", validateQuery(v.postListSchema), p.list); router.post("/", createRateLimit({ max: 20, code: "SAFE_PLACE_POST_RATE_LIMIT" }), validateBody(v.postCreateSchema), p.create); router.get("/:postId", validateParams(v.postIdSchema), p.detail); router.patch("/:postId", validateParams(v.postIdSchema), validateBody(v.postUpdateSchema), p.update); router.delete("/:postId", validateParams(v.postIdSchema), p.remove); router.post("/:postId/correction", validateParams(v.postIdSchema), validateBody(v.postUpdateSchema), p.correction); router.get("/:postId/comments", validateParams(v.postIdSchema), c.list); router.post("/:postId/comments", createRateLimit({ max: 60, code: "SAFE_PLACE_COMMENT_RATE_LIMIT" }), validateParams(v.postIdSchema), validateBody(v.commentSchema), c.create); router.put("/:postId/reaction", createRateLimit({ max: 120 }), validateParams(v.postIdSchema), validateBody(v.reactionSchema), r.postSet); router.delete("/:postId/reaction", validateParams(v.postIdSchema), r.postRemove); module.exports = router;
