const ACCOUNT_TOKEN_TYPES = Object.freeze({
  EMAIL_VERIFICATION: "EMAIL_VERIFICATION",
  LOGIN_LINK: "LOGIN_LINK",
});

const AUTH_DURATIONS = Object.freeze({
  EMAIL_VERIFICATION_MS: 24 * 60 * 60 * 1000,
  LOGIN_LINK_MS: 15 * 60 * 1000,
  RESEND_COOLDOWN_MS: 5 * 60 * 1000,
});

module.exports = { ACCOUNT_TOKEN_TYPES, AUTH_DURATIONS };
