const mongoose = require("mongoose");
const schema = new mongoose.Schema({ fixtureKey: { type: String, default: null, index: true, select: false }, user: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true, index: true }, suspendedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true }, reason: { type: String, required: true, trim: true, maxlength: 2000 }, startsAt: { type: Date, default: Date.now }, endsAt: { type: Date, default: null, index: true }, status: { type: String, enum: ["ACTIVE", "EXPIRED", "LIFTED"], default: "ACTIVE", index: true }, liftedAt: { type: Date, default: null }, liftedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User", default: null }, liftReason: { type: String, trim: true, maxlength: 1000, default: null } }, { timestamps: true });
schema.index({ user: 1, status: 1 }, { unique: true, partialFilterExpression: { status: "ACTIVE" } }); module.exports = mongoose.model("SafePlaceSuspension", schema);
