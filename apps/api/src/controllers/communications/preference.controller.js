const service = require("../../services/communications/preference.service"); const wrap = (fn) => async (req, res, next) => { try { await fn(req, res); } catch (e) { next(e); } };
module.exports = { mine: wrap(async (req, res) => res.json({ success: true, preferences: service.publicData(await service.forUser(req.auth.user)) })), update: wrap(async (req, res) => res.json({ success: true, preferences: service.publicData(await service.updateForUser(req.auth.user, req.body)) })), unsubscribeDetails: wrap(async (req, res) => res.json({ success: true, preferences: await service.unsubscribeDetails(req.query.token) })), unsubscribe: wrap(async (req, res) => res.json({ success: true, preferences: await service.unsubscribe(req.body) })), requestResubscribe: wrap(async (req, res) => res.json({ success: true, ...(await service.requestResubscribe(req.body.email, req.body.preferences)) })), confirmResubscribe: wrap(async (req, res) => res.json({ success: true, preferences: await service.confirmResubscribe(req.body.token) })) };
