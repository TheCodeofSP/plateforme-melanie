const jobs = require("../services/cron.service"); const wrap = (job) => async (req, res, next) => { try { res.json({ success: true, result: await job() }); } catch (e) { next(e); } };
module.exports = { publishResources: wrap(jobs.publishResources), retryQuizDeliveries: wrap(jobs.retryQuizDeliveries), cleanupIncompleteQuizzes: wrap(jobs.cleanupIncompleteQuizzes), cleanupExpiredGuestQuizzes: wrap(jobs.cleanupExpiredGuestQuizzes), cleanupApplicationDocuments: wrap(jobs.cleanupApplicationDocuments), cleanupMedia: wrap(jobs.cleanupMedia), expireSafePlaceSuspensions: wrap(jobs.expireSafePlaceSuspensions), cleanupSafePlaceNotifications: wrap(jobs.cleanupSafePlaceNotifications), cleanupSafePlaceDeletedContent: wrap(jobs.cleanupSafePlaceDeletedContent), cleanupSafePlaceMedia: wrap(jobs.cleanupSafePlaceMedia), webinarReminders24: wrap(jobs.webinarReminders24), webinarReminders1: wrap(jobs.webinarReminders1), maintainWebinars: wrap(jobs.maintainWebinars), sendScheduledCommunications: wrap(jobs.sendScheduledCommunications), processCommunicationBatches: wrap(jobs.processCommunicationBatches), retryCommunicationDeliveries: wrap(jobs.retryCommunicationDeliveries), anonymizeOldCommunicationRecipients: wrap(jobs.anonymizeOldCommunicationRecipients), cleanupCommunicationMedia: wrap(jobs.cleanupCommunicationMedia), processNotificationDeliveries: wrap(jobs.processNotificationDeliveries), cleanupNotifications: wrap(jobs.cleanupNotifications), notifyDashboardTasks: wrap(jobs.notifyDashboardTasks), expireDashboardInvitations: wrap(jobs.expireDashboardInvitations), processDashboardExports: wrap(jobs.processDashboardExports), cleanupDashboardExports: wrap(jobs.cleanupDashboardExports) };
