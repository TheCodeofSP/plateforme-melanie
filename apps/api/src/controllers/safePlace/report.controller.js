const service = require("../../services/safePlace/report.service"); const wrap = (fn) => async (req, res, next) => { try { await fn(req, res); } catch (e) { next(e); } }; module.exports = { create: wrap(async (req, res) => res.status(201).json({ success: true, message: "Signalement transmis.", report: await service.create(req.auth.user, req.body) })), mine: wrap(async (req, res) => res.json({ success: true, ...(await service.mine(req.auth.user._id, req.validatedQuery)) })), queue: wrap(async (req, res) => res.json({ success: true, ...(await service.queue(req.validatedQuery)) })), detail: wrap(async (req, res) => res.json({ success: true, ...(await service.detail(req.params.reportId)) })), review: wrap(async (req, res) => res.json({ success: true, report: await service.takeReview(req.params.reportId, req.auth.user) })), keep: wrap(async (req, res) => res.json({ success: true, report: await service.keep(req.params.reportId, req.auth.user, req.body.reason) })), hide: wrap(async (req, res) => res.json({ success: true, report: await service.hide(req.params.reportId, req.auth.user, req.body.reason) })), correction: wrap(async (req, res) => res.json({ success: true, report: await service.requestCorrection(req.params.reportId, req.auth.user, req.body.reason) })), close: wrap(async (req, res) => res.json({ success: true, report: await service.closeDiscussion(req.params.reportId, req.auth.user, req.body.reason) })), suspend: wrap(async (req, res) => res.json({ success: true, ...(await service.suspendAuthor(req.params.reportId, req.auth.user, req.body)) })) };
