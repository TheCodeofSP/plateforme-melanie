const service = require("../../services/safePlace/suspension.service"); const wrap = (fn) => async (req, res, next) => { try { await fn(req, res); } catch (e) { next(e); } }; module.exports = { list: wrap(async (req, res) => res.json({ success: true, ...(await service.list(req.validatedQuery)) })), suspend: wrap(async (req, res) => res.status(201).json({ success: true, suspension: await service.suspend(req.auth.user, req.params.userId, req.body) })), lift: wrap(async (req, res) => res.json({ success: true, suspension: await service.lift(req.auth.user, req.params.suspensionId, req.body.reason) })), history: wrap(async (req, res) => res.json({ success: true, ...(await service.history(req.params.userId)) })) };
